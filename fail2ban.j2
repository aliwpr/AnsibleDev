[DEFAULT]

bantime  = 10m

findtime  = 10m
maxretry = 5


ignoreip = 127.0.0.1/8 {{ allowed_ips | join(' ') }}

backend = auto

[sshd]
enabled = true
port    = {{ ssh_port }}
filter  = sshd
logpath = /var/log/auth.log
maxretry = 6